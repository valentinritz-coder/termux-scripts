TS=$(date +%F_%H-%M-%S)
LOG="/sdcard/cfl_watch/logs/map_${TS}.log"
mkdir -p /sdcard/cfl_watch/logs

bash /sdcard/cfl_watch/map_run.sh --depth 1 --max-screens 5 --max-actions 3 2>&1 | tee "$LOG"

---

ls -1dt /sdcard/cfl_watch/map/* | head -n 5
